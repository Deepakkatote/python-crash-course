username = input("username: ")
password = input("password: ")



current_username = []
for current_usernames in current_username:
    username.append() == current_username
    if current_usernames == current_username:
        print("usename is already available")

print(username)
print(password)